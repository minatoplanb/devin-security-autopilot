services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      - NODE_ENV=production
    volumes:
      - ./output:/app/output
      - ./data:/app/data
    restart: unless-stopped
